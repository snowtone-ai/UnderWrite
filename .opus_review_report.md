# レビュー報告 — feat/commercial-hardening (PR #17)

## レビュー対象
`git diff main...feat/commercial-hardening`（15ファイル, +524/-99）。
Fable 5 による commercial-hardening コミット 1件（テナント分離・サーバ側ガード・エンジン境界値）。
差分に加え、直接相互作用する未変更コード（photos/status ルート、scan-flow のリトライ、engine の verdict、Gemini の MIME 処理）も精査した。

## 差分の評価（総評）
コミット本体の品質は高い。以下は正しく実装されていることを確認した:
- **テナント分離（IDOR 対策）**: photos / status / scans の全ルートで `getSessionUser()` による認証と `scan.user_id === user.id` の所有権チェックを追加。service-role クライアントで RLS がバイパスされる前提に対し、ルート単位の境界が正しく敷かれている。
- **パスインジェクション対策**: `SLOT_RE = /^[a-z0-9_-]{1,32}$/` により `slot` を検証。`../evil` 等の storage path 汚染を遮断（テスト有）。
- **リソース濫用対策**: `MAX_PHOTO_BYTES`(10MB / 413), `MAX_PHOTOS_PER_SCAN`(20 / 409), MIME allowlist（415）。
- **AI 境界の MIME 修正**: `AIProvider.analyzeImages` を `AIImage[]`（base64 + mimeType）へ変更し、Gemini の inlineData に実 MIME を伝播。従来 jpeg 固定だった png/webp/heic が正しく扱われる。lib/ai/ 経由のみで完結し、invariant を維持。
- **エンジン境界値**: `ENGINE_VERSION` を 1.1.0 に更新。`resaleMedian === 0` の NaN margin を回避、`purchaseCapYen <= 0` を常に nogo に矯正（P50 ベースの margin が正でも成立しない買付価格を go にしない）。pure/deterministic を維持し lib/ai を import していない。

## 発見した問題と対応

### [中] photos ルート: DB insert 失敗時のストレージ孤児（修正済み）
storage upload 成功後に `photos` 行 insert が失敗すると、アップロード済み blob がバケットに孤児として残留する（データ整合性・ストレージコストの問題）。
**修正**: insert 失敗パスで `db.storage.from("property-photos").remove([storagePath])` を実行。パス一致を検証する回帰テストを追加。

### [低〜中・見送り] status ルート: 同時ポーリングによるエンジン二重実行
全写真完了後、複数タブ／リロード／React StrictMode により GET が同時到達すると、両方が `existing?.result` チェックを通過し `fetchResaleBaseline`（外部API・レート制限あり）＋ `runEngine` ＋ upsert を二重実行しうる。
**見送り理由**: engine は決定的、upsert は `onConflict: "scan_id"` で冪等のため**結果は不正化しない**。実害は稀な外部API重複コールのみ。真にアトミックな claim には検証不能な DB制約／`scans.status` enum（`pending|done|failed` で拘束）の変更が必要で、スコープ外かつバグより高リスク。単一クライアントのポーリングは逐次のため通常経路では発生しない。運用上は不動産情報ライブラリ側のレート制限で吸収可能。将来的に `underwritings` へ claim 用の一意行 insert で対処するのが望ましい旨を記録。

### [低・見送り] scan-flow リトライ: メタデータ再送されず
部分アップロード失敗後の再送は `scanIdRef` により scan を再作成しないため、失敗後にユーザが住所/築年を編集して再送信しても編集が無視される。また `uploadedSlotsRef` は slot キーのため、同一 slot の撮り直しはスキップされる。
**見送り理由**: この画面の目的（「失敗分だけ再送」）は正しく実装されている。成功時は `router.push` で離脱・ref はアンマウントで消える。編集後再送は稀なエッジで、メタデータ同期や slot 差し替えの追加は機能拡張（スコープ外）。

### 確認したが問題なしと判断した点
- `MAX_PHOTOS_PER_SCAN` の count チェックは TOCTOU だが、上限は soft guard であり同時2枚の超過は許容範囲。
- `contentType` 空文字時の `|| "image/jpeg"` フォールバックは妥当。`image/jpg` 等の非標準を 415 で弾く厳格さも妥当。
- verdict が nogo だが `expectedMarginPct` が正になるケース（cap<=0）は headline が cap<=0 分岐で正しく整合。

## 適用した修正
- `app/api/scans/[scanId]/photos/route.ts`: insert 失敗時の storage `remove` によるクリーンアップ。
- `app/api/scans/[scanId]/photos/route.test.ts`: `insertOk` オプションと `mockRemove` を追加、孤児削除の回帰テストを追加。

コミット: `5f88115 fix: clean up orphaned storage blob on photo insert failure`（feat/commercial-hardening に push 済み）。

## 最終検証結果（pnpm verify）
```
=== pnpm lint ===       ✓ lint passed
=== pnpm typecheck ===  ✓ typecheck passed
=== pnpm test ===       Test Files 12 passed (12) / Tests 78 passed (78) ✓
=== pnpm build ===      ✓ build passed
verify: ALL PASSED
```
全ステージ green。PR はマージせず、main へのコミットは行っていない。
